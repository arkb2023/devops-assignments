apiVersion: v1
kind: Service
metadata:
  name: website-svc
  labels:
    app: website
spec:
  type: NodePort
  selector:
    app: website
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30008
    protocol: TCP
    name: http
