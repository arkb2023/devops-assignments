[defaults]
host_key_checking = False
remote_user = ubuntu
private_key_file = ~/.ssh/
retry_files_enabled = False
roles_path = roles
inventory = inventory/aws-managed.ini

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False